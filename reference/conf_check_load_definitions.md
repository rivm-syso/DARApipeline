# check and load definitions

Helper function to read object definitions

## Usage

``` r
conf_check_load_definitions(f, .call = parent.frame())
```

## Arguments

- f:

  Path: filepath

- .call:

  Object: the parent environment from return_env, internal - do not use

## Value

yaml
