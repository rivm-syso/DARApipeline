# Save image

Helper function that saves an image.

## Usage

``` r
save_image(object, bestand, device, ...)
```

## Arguments

- object:

  Object to be saved as image

- bestand:

  File name to create on disk

- device:

  Device to use. Parameter to be used within ggsave
