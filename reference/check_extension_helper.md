# Check extension (helper)

This function checks if the desired extension is present or not. If the
extension is not present, it concatenated it to the filenmae

## Usage

``` r
check_extension_helper(filename, ext)
```

## Arguments

- filename:

  string, the filename

- ext, :

  string, the extension to check for

## Value

filename with extension
