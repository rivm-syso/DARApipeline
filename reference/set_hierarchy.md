# Set hierarchy

Helper function to set hierarchy for `conf_create_dag`

## Usage

``` r
set_hierarchy(gr, call = parent.frame())
```

## Arguments

- gr:

  Graph: Grab object that holds DAG

- call:

  the parent environment from return_env, internal - do not use

## Value

hierarchy_level
