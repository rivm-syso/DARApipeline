# Check tags

Helper function used to check tags

## Usage

``` r
check_tags(tags, p_e, call = parent.frame())
```

## Arguments

- tags:

  List: list of tags to check

- p_e:

  Object: package_environment, internal - do not use

- call:

  Object: the parent environment from return_env, internal - do not use

## Value

None
