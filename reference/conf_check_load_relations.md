# Check and load relations

Helper function to read object relations

## Usage

``` r
conf_check_load_relations(f, .call = parent.frame())
```

## Arguments

- f:

  Path: filepath

- .call:

  Object: the parent environment from return_env, internal - do not use

## Value

yaml
