# Check and load project paths

Helper function to read project paths

## Usage

``` r
conf_check_load_proj_paths(f, .call = parent.frame())
```

## Arguments

- f:

  Path: filepath

- .call:

  Object: the parent environment from return_env, internal - do not use

## Value

yaml
