# Argument check for get sp data

Helper function that checks connection arguments

## Usage

``` r
get_sp_data_argcheck(con_specs, call = parent.frame())
```

## Arguments

- con_specs:

  List: list with connection specifications

- call:

  Object: the parent environment from return_env, internal - do not use

## Value

checked arguments
